const Getters = {
  GetSelectDept(state) {
    return state.selectDept;
  },
  GetNoRelateUser(state) {
    return state.noRelateUser;
  },
  GetRobotList(state) {
    return state.robotList;
  }
};

export default Getters;
