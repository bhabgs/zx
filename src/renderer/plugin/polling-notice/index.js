import PollingNotify from "./polling-notify";
import PollingReadState from "./polling-readstate";
import PollingOutSideNotify from "./polling-notify-outside"; // 外网通知轮询
import PollingMailUnRead from "./polling-mail-unread";  // 轮询智邮未读数据
import PollingActionUnRead from "./polling-action-unread";  // 行动中心未读数据
import PollingExtCorpInfo from "./polling-corp-info";

export { 
  PollingNotify, 
  PollingReadState, 
  PollingOutSideNotify, 
  PollingMailUnRead, 
  PollingActionUnRead, 
  PollingExtCorpInfo 
};

export default { 
  PollingNotify, 
  PollingReadState, 
  PollingOutSideNotify, 
  PollingMailUnRead, 
  PollingActionUnRead, 
  PollingExtCorpInfo 
};
